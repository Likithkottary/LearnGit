
#configuration part
git config --list
git config --global user.name "name"
git config --global user.email "mail"
git config --list


git status
git init #initialize repo
git status

touch home.html
git add home.html #add to staged area
git restore --staged home.html #remove from staged area
git status
git add -A #add all files to SA
git commit -m "message" #Commit with message

git checkout home.html #restores single files
git checkout -f  #restores multiple files 
git log 
git log -p -1 #checks for number of logs
git diff #compares working tree with staging area
git diff --staged

git commit -a -m "skipped adding shortcut" #cando both add and commit